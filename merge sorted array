class Main {
    
    public static void merge(int[]c,int a[], int b[]){
        int i=0;
        int j=0;
        int k=0;
        while(i<a.length && j<b.length){
            if(a[i]<b[j])  c[k++]=a[i++];
            else  c[k++]=b[j++];
            
        }
        if(i==a.length){// a arraay kahatm ho gya ==>to bacha hua j ko c array me add kr do
        
            while(j<b.length){
                c[k++]=b[j++];
            }
        }
        else{
             c[k++]=a[i++];
        }
        
        // while(i<a.length){
        //      c[k++]=a[i++];
        // }
        //  while(j<b.length){
        //      c[k++]=b[j++];
        // }
    }
    public static void main(String[] args) {
        int[] a={2,5,6,9};
        int[] b={1,3,4,5,6,7,8,90,676};
        int [] c= new int [a.length+b.length];
        for(int nums:c){
           System.out.print(nums+" ");
           
        }
        System.out.println();
        merge(c,a,b);
           for(int nums:c){
           System.out.print(nums+" ");
        }
        System.out.println();
        
        
       
    }
}
